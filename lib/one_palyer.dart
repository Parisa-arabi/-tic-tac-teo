import 'package:flutter/material.dart';

class one_player extends StatefulWidget {
  const one_player({Key? key}) : super(key: key);

  @override
  State<one_player> createState() => _one_playerState();
}

class _one_playerState extends State<one_player> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
